language: node_js
node_js:
  - "0.11"
  - "0.10"
before_script:
  - npm install -g grunt
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install
  - bower install
  - cd ./bower_components/handlebars
  - grunt build
  - cd ../../
  - grunt build
script: grunt test --verbose --force
