language: node_js
node_js:
  - "0.12"
sudo: false
addons:
  sauce_connect:
    username: reboundjs
    access_key: 07875532-956f-448b-a77d-1520ac359f66
before_script:
  - npm install -g bower
  - "[[ $SUITE = saucelabs ]] && npm install -g saucie || true"
  - "[[ $SUITE = saucelabs ]] && npm install -g testem@0.8.3 || true"
env:
  - SUITE=phantomjs
  - SUITE=saucelabs
script: "[[ $SUITE = phantomjs ]] && npm test || testem ci"
