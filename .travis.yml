language: node_js
node_js:
  - "0.12"
sudo: false
addons:
  sauce_connect:
    username: reboundjs
    access_key: 07875532-956f-448b-a77d-1520ac359f66
before_script:
  - npm install -g bower
  - "[[ $SUITE = phantomjs ]] && npm install -g gulp"
  - "[[ $SUITE = saucelabs ]] && npm install -g saucie"
  - "[[ $SUITE = saucelabs ]] && npm install -g testem"
env:
  - SUITE=phantomjs
  - SUITE=saucelabs
script: "[[ $SUITE = phantomjs ]] && gulp test || testem ci"
