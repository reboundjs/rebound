import { Model, Collection } from 'rebound-data/rebound-data';

QUnit.skip('[Rebound Data] Model – Url', function(assert) {
  var model;

// Shallow Toggle Boolean Values
  model = new Model({});

});
